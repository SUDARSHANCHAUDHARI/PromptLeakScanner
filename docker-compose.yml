services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    volumes:
      - ./data/reports:/app/data/reports
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.web
